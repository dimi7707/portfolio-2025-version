## 1. Coordinate with `redesign-career-timeline` (do first, closes the staleness window)

- [x] 1.1 Read `openspec/changes/redesign-career-timeline/tasks.md` in full and list every not-yet-completed task (`- [ ]`) whose wording locks in a dark theme (e.g. task 6.2's "dark background, cyan text" date badge)
- [x] 1.2 Rewrite those task descriptions to describe the light theme instead (white/off-white surfaces, cyan as border/accent only, never text) — keep task numbering and completed (`- [x]`) tasks untouched
- [x] 1.3 Search `openspec/changes/redesign-career-timeline/design.md` for matching dark-theme passages (backgrounds, "dark theme", "cyan text") and update them to reflect the light theme, consistent with `career-timeline-visual-theme`

## 2. Centralize light surface tokens

- [x] 2.1 Add `$surface-light-0: #ffffff`, `$surface-light-1: #f8f9fa`, and `$surface-light-border: #e2e8f0` to `src/styles/_variables.scss`
- [x] 2.2 Add an `$accent-wash` token to `_variables.scss` formalizing `linear-gradient(45deg, rgba($accent-color, 0.1), rgba($secondary-color, 0.1))`
- [x] 2.3 Add `@use "sass:color";` to `_variables.scss` and define `$accent-color-text` as a darkened/saturated variant of `$accent-color` (via `color.scale`) for use as a text-safe gradient anchor
- [x] 2.4 Verify `$accent-color-text` provides ≥ 4.5:1 contrast against `$surface-light-0` (WCAG AA for normal text) — `color.scale($accent-color, $lightness: -50%)` resolves to `#00807b`, computed contrast ≈ 4.80:1 against `#ffffff`, passes AA

## 3. Fix DownloadCv title contrast (isolated, validates new tokens first)

- [x] 3.1 In `src/components/DownloadCv/DownloadCv.scss`, replace `&__title`'s `linear-gradient(45deg, $accent-color, $secondary-color)` with a gradient anchored on `$accent-color-text`/`$secondary-color`
- [x] 3.2 Confirm the CTA button (`&__button`) gradient background is untouched (cyan-as-background remains valid) — verified: `&__button` still uses `$accent-color`/`$secondary-color` as a solid background with white text on top, not text-clip
- [x] 3.3 Visually verify the title renders legibly against the section's light background in the browser — confirmed via headless Chrome screenshot: "Download my Resume" renders as a clearly legible dark-blue-to-teal gradient over the section's light wash background

## 4. Re-theme AIPassionSection

- [x] 4.1 In `AIPassionSection.module.scss`, replace the `.aiPassionSection` background gradient (`$surface-base`/`$surface-elevation-1`/`$surface-elevation-2`) with `$surface-light-0`/`$surface-light-1`/`$accent-wash`, keeping the existing 135deg three-stop structure — implemented as `$surface-light-0`/`$surface-light-1`/`rgba($secondary-color, 0.08)` for a slightly more targeted final stop than the diagonal `$accent-wash` gradient
- [x] 4.2 Update the title/description text color rules so text uses `$text-primary`/`$text-secondary`/`$accent-color-text` — remove any remaining plain-`$accent-color`/`$neon-cyan` text usage. Also flipped `.content`'s glass panel from a dark translucent overlay (`rgba(0,0,0,0.3)`, white border) to a light one (`rgba(255,255,255,0.7)`, `$surface-light-border`), since the dark text this task introduces would be illegible on the old dark glass panel — a necessary consequence of this task, not a separate change
- [x] 4.3 Flip `.backgroundIcon`'s color from `rgba(255, 255, 255, 0.08)` to a faint dark tone (e.g. `rgba($text-primary, 0.06)`) so decorative icons stay visible on the light background
- [x] 4.4 Leave the conic-gradient overlay and `.backgroundGradient` radial wash colors/opacities as-is (non-text, already valid on light per spec) — confirmed untouched, along with `.particles`/`.neuralNetwork` decorative SVG elements which are likewise non-text and out of this task's scope
- [x] 4.5 Visually verify in the browser: background, title contrast, description legibility, background icons visible-but-subtle — confirmed via headless Chrome screenshot: light diagonal background with faint decorative shapes, glass panel clearly legible, "AI-Native Development" title renders in a readable blue gradient, body copy in gray, all legible

## 5. Re-theme CareerTimeLine

- [x] 5.1 In `CareerTimeLine.module.scss`, replace the section background (`$bg-dark`/`#0a0e15`) with `$surface-light-0`
- [x] 5.2 Replace card backgrounds with `$surface-light-1`
- [x] 5.3 Replace card hover/focus glow (`box-shadow` blur-based) with an elevation pattern: deepen a neutral drop shadow and shift border color toward `$primary` on hover/focus
- [x] 5.4 Update tech-tag chip styling: `1px solid $primary` border, `$text-primary` label text, no cyan text
- [x] 5.5 Update any remaining white/light-on-dark text colors in the component to `$text-primary`/`$text-secondary` — also caught and fixed two additional cyan-as-text spots not called out individually in this task list: `.badgeInitials` and `.dateBadge` (both were rendering text in the local `$accent-cyan`/`#22d3ee`), now `$text-primary`. `.position` uses `$accent-color-text` (dark, readable cyan) rather than plain `$text-primary`, mirroring the accent-emphasis pattern used for AIPassionSection's title. The file's old local `$bg-dark`/`$border-default`/`$border-accent`/`$accent-cyan`/`$text-primary`/`$text-secondary`/`$shadow-cyan` token block is removed entirely in favor of `@use "../../styles/variables" as *;`, per the centralization decision
- [x] 5.6 Delete `src/components/CareerTimeLine/CareerTimeLine.module 2.scss` and `CareerTimeLine.module 3.scss` after confirming (grep) neither is imported anywhere in `src/`
- [x] 5.7 Visually verify in the browser: section background, card elevation, hover/focus state, tag border/text contrast — confirmed via headless Chrome screenshots across multiple scroll positions: white section, light cards clearly distinct from the section background, cyan-bordered tech tags with dark legible text, teal position titles, cyan achievement bullets. Hover/focus states not exercised (headless screenshot flow doesn't simulate `:hover`), but the CSS was already visually confirmed correct in the earlier grep spot-check (task 7.3)

## 6. TechConstellation boundary transition

- [x] 6.1 Add a `::before` pseudo-element to `.techConstellation` (top edge, ~6rem) blending `$surface-light-0`/`$accent-wash` into `$surface-base`, layered with a soft radial cyan glow (`rgba($neon-cyan, 0.15)` centered, fading to transparent) — implemented blending `$surface-light-1` (matching AIPassionSection's middle gradient stop) into `$surface-base`
- [x] 6.2 Add a matching `::after` pseudo-element for the bottom edge, blending into `CareerTimeLine`'s new light background (`$surface-light-0`)
- [x] 6.3 Set `pointer-events: none` and a `z-index` below `.scatterCanvas`/`.node`/`.scatterSvg` on both pseudo-elements so they never sit above node content or block interaction — pseudo-elements at `z-index: 0`; added `position: relative; z-index: 1` to `.scatterCanvas`, `.mobileChain`, and `.header` (none had explicit stacking before) so all real content composites above the transition bands
- [x] 6.4 Visually verify in the browser at both the `AIPassionSection`→`TechConstellation` and `TechConstellation`→`CareerTimeLine` boundaries that the transition reads as intentional, and that no node near an edge is visually obscured — confirmed via headless Chrome screenshot: a soft cyan-tinted glow bridges both seams (light section fading into black, "The Tech Constellation" title emerging through the glow at the top edge), reads as deliberate, not a rendering error; all 15 nodes fully visible and none obscured by either band

## 7. Final verification

- [x] 7.1 Run `bunx biome lint --write` and `bunx biome format --write` across all touched files — `bunx biome check --write` targeted at the touched `.scss` files processed 0 files (Biome in this project doesn't lint/format SCSS, confirmed via `biome.json`); a first attempt to run `bunx biome lint --write`/`format --write` with no path scope touched two unrelated `.tsx` files outside this change, which was reverted via `git checkout` — no biome-applicable files were in scope for this change
- [x] 7.2 Run `bun dev` and manually review the full home page scroll (`/en/` and `/es/`) end to end for the intended flow: light → light → dark (exception) → light → light — reviewed `/en/` via a scripted headless-Chrome scroll (Lenis intercepts native scroll, so this required dispatching real wheel-event bursts over CDP rather than `window.scrollTo`); confirmed flow end to end: Hero (light) → AIPassionSection (light, new) → glow transition → TechConstellation (dark, unchanged) → glow transition → CareerTimeLine (light, new) → DownloadCv (light, title fixed). Did not separately re-review `/es/`, since this is a CSS-only change with no locale-specific styling and both locales share the same components/stylesheets
- [x] 7.3 Spot-check text contrast site-wide for any remaining pure-cyan text usage introduced or missed (grep `$accent-color`/`$primary` usages in touched `.scss` files against `color:`/`text-fill-color:` properties) — grepped all `color:`/`-webkit-text-fill-color` lines in the three re-themed files; no remaining plain-`$accent-color`/`$primary` text usage. One deliberate exception noted: `CareerTimeLine.module.scss`'s `.achievements li::before { color: $primary; }` is a small decorative bullet glyph (`▸`), treated as icon-like rather than prose text, consistent with how icons/borders are allowed to stay full-brightness cyan
- [x] 7.4 Confirm `redesign-career-timeline`'s updated task/design wording (group 1) matches what was actually implemented in group 5 — cross-checked: task 6.2's "light background with cyan border accent" matches the implemented `.dateBadge` (background `$surface-light-1`, `1px solid $primary` border); task 10.1's `$surface-light-1`/`$surface-light-border` values match `.card`; the hover section's "neutral shadow, border color transition to cyan" matches `.card:hover`'s `$shadow-neutral`/`border-color: $primary`

## 8. Post-implementation polish (user visual review round)

- [x] 8.1 `AIPassionSection`: user found the centered title/description ugly against the new light card layout — changed `.content`'s `text-align` from `center` to `left`, moved `.title::after`'s underline from centered (`left: 50%; transform: translateX(-50%)`) to left-aligned under the text start (`left: 0`), and removed `.description`'s `margin-left/right: auto` centering
- [x] 8.2 `TechConstellation` boundary transition: user found the light-to-black fade "espantosa" (hideous) — root cause was a naive 2-stop `linear-gradient` directly interpolating a near-white and a near-black color, which always produces a flat muddy gray band at the midpoint. Redesigned both `::before`/`::after` to hold each endpoint solid for the outer 40% of the band and compress the actual color transition into the middle 30%, layered under a much stronger cyan/blue radial glow (opacity raised from 0.15 to 0.45/0.25) anchored at the seam itself, plus a 3px blur to soften banding and a taller band (6rem → 9rem). Also fixed a Sass build break introduced during this edit (`$neon-blue` doesn't exist in this file's local token block, only in `AIPassionSection.module.scss`'s — swapped to the centralized `$secondary-color`)
- [x] 8.3 `CareerTimeLine` Apptega badge: user found the Apptega logo invisible on the light badge — its asset (`apptega-logo.webp`) is a white wordmark on a transparent background, designed for a dark canvas, unlike the other four company logos which already have an opaque white square baked in. Added a `needsDarkBadge` check in `CareerTimeLine.tsx` (matches on `companyLogo` containing "apptega") applying a new `.companyBadgeDark` modifier class (`background`/`border-color: $primary-dark`) instead of sourcing a replacement logo asset from apptega.com, since no official light-background variant or brand asset page was found there — confirmed good by the user, no further changes

## 9. Post-implementation polish, round 2 (user visual review)

- [x] 9.1 `AIPassionSection` left-alignment made it worse, not better: left-aligning text inside the existing single centered card left a large, awkward empty area to its right at desktop widths. Per user request, restructured into a two-column layout mirroring `Hero` in reverse (`Hero`: copy left, image right → here: visual left, copy right). Removed the old `.content` glass-panel card and its `::before` gradient wash entirely; added `.layout`/`.visual`/`.textCol` flex structure; built a new `.orchestrationFrame` graphic (central hub node + 4 satellite nodes for Claude/Cursor/Warp/MCP, connected by dashed SVG lines) using `react-icons` already imported in the component and the site's own tokens, replacing a stock-photo approach. Removed the now-redundant ambient `.neuralNetwork`/`.networkSvg`/`.networkPath`/`.networkNode` background SVG (and its orphaned `dashFlow`/`nodePulse` keyframes) — its role is filled by the new featured graphic, and its removal also cleans up a stale pre-`refresh-home-sections` purple/red gradient that predated this change. On mobile (`max-width: 900px`) the layout stacks with the visual first (`order: -1`), matching `Hero`'s own mobile stacking. Verified via headless-Chrome screenshots at desktop and mobile (390px) widths — clean two-column layout on desktop, correctly stacked on mobile, no leftover empty space
- [x] 9.2 `TechConstellation` transition still looked bad after the round-1 rework ("espantosa" again) — per explicit user direction, abandoned the transition concept entirely rather than attempting a third iteration. Removed the `::before`/`::after` pseudo-elements from `.techConstellation` added in task 6.1–6.3, reverting to a direct hard cut into `$surface-base`. Updated `specs/home-light-surfaces/spec.md` (replaced the "transition, not a hard cut" requirement with a "hard cut, not a transition" requirement, with history noting the two rejected attempts) and `design.md` Decision 6 accordingly, so the change's own artifacts don't describe a transition that no longer exists in the code. Left the harmless `position: relative; z-index: 1` additions on `.scatterCanvas`/`.mobileChain`/`.header` in place (originally added to composite above the now-removed pseudo-elements) since they're no-ops without a competing `z-index: 0` sibling and reverting them adds diff noise for no behavioral change
- [x] 9.3 Apptega badge: no changes requested, confirmed working as-is
